import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    borderRadius: 6,
    backgroundColor: "#FF79E5",
    marginBottom: 30,
  },
  text: {
    padding: 10,
    textAlign: "center",
    color: "white",
  },
});
